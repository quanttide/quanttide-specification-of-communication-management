# AGENTS.md - Agent 工作指南

本文档为 Agent（包括 CodeBuddy、Claude Code、Cursor、GitHub Copilot 等）在本仓库中工作提供指南。

## 核心记忆

### 项目定位

本仓库是量潮沟通管理标准仓库，定义沟通管理领域的工程标准：数据模型、接口契约与规范约定。

### 核心架构思想

1. **正交分解**：分离"语境"（context/）、"媒介"（medium/）与"内容"（content/）
2. **单一事实源**：每个概念定义独立文件，父仓库只追踪引用
3. **知识即代码**：标准文档统一版本控制
4. **统一规范**：所有定义遵循统一结构（定义、领域模型、领域事件、API规格）

### 子模块管理
- 子模块独立维护，父仓库只追踪引用
- **禁止**：直接在父仓库修改子模块文件
- **必须**：在子模块仓库独立提交推送，父仓库只更新引用

### 关键文档索引

| 文档 | 用途 |
|------|------|
| [README.md](README.md) | 快速开始、目录结构 |
| [CHANGELOG.md](../CHANGELOG.md) | 版本变更记录（位于父仓库） |

### 人机协作范式

1. **最小干预**：仅在用户明确请求时操作
2. **信息复用**：优先使用已有文档内容
3. **查询契约**：维护前先读取现有文档结构
4. **维护记录**：修改后同步更新 CHANGELOG.md
5. **验证优先**：完成后检查文档格式和内容一致性
6. **原子提交**：每次提交包含完整独立变更

## 必查文档清单

### 首次进入项目时必读

| 顺序 | 文档 | 查阅内容 |
|------|------|----------|
| 1 | [README.md](README.md) | 项目结构、目录结构 |
| 2 | 本文件 (AGENTS.md) | 人机协作范式、工作流程 |

### 执行具体任务时必查

| 任务类型 | 必查文档 | 查阅内容 |
|----------|----------|----------|
| **修改定义文档** | 所在目录的 `index.md` | 内容结构、关联关系 |
| **添加新定义** | 现有定义文档 | 定义风格、格式规范 |
| **修改API规格** | 现有API规格文档 | API设计规范、示例格式 |
| **提交变更** | [CHANGELOG.md](../CHANGELOG.md) | 版本记录格式、最新版本 |

## 文档结构规范

### 定义文档结构

每个定义文档应遵循以下结构：

```markdown
# 概念名称 (Concept Name)

## 定义

在沟通管理领域，**概念名称**是指...

## 领域模型

### 概念名称（概念）

- **字段名** (类型)：字段说明

## 领域事件

### 事件名称（事件描述）

- **发生时机**：事件触发条件
- **事件载荷**：事件数据结构
- **下游影响**：事件触发后的后续操作

## API 规格

### 概念 API

#### 操作名称
- **HTTP方法** `/路径`
- **使用场景**：操作的应用场景
- **请求体**：请求数据结构
- **响应**：响应数据结构
```

### 格式规范

1. **使用列表格式**：字段定义使用列表格式，而不是表格格式
2. **实际示例**：JSON示例使用实际数据，而不是"string"、"uuid"等占位符
3. **使用场景**：每个API操作都应有使用场景说明
4. **领域事件**：每个领域事件都应有发生时机、事件载荷和下游影响

## AI 执行指引

开始工作前阅读现有文档，了解定义风格和格式规范。

过往经验表明 AI 最容易在这些地方犯错，请特别注意：

- **替换 vs 并存**：修改内容前先确认旧内容怎么处理，不要默认替换
- **改内容 vs 改名字**：操作前明确目标是文件/目录的内容还是名称
- **子仓库 vs 主仓库**：不要在子仓库里做主仓库的事（如改父仓库的 ROADMAP），也不要反过来
- **减法优先**：删除无效内容的优先级高于新增，宁可空着不放不确定的东西
- **目录即语义**：目录名使用代码实际结构，不用抽象概念。有实体才建目录，但预留目录也是有效的架构声明——不要擅自移除用户放置的空目录和占位文件
- **提交即推送**：提交后默认推送到远端（主仓库和子模块都推），除非用户明确说“只提交不推”
- **格式一致性**：保持文档格式的一致性，遵循现有文档的风格

### 特殊文档提醒

完成重要变更后，检查是否需要同步更新以下文档：

| 文档 | 触发条件 |
|------|---------|
| [CHANGELOG.md](../CHANGELOG.md) | 对用户可见的变更（新功能、重构、修复） |
| [README.md](README.md) | 产品功能或仓库结构变化 |
| [AGENTS.md](AGENTS.md) | AI 工作经验增加 |

AI 应在完成工作后主动提醒用户是否需要更新这些文档，而非等用户提出。

## 定义风格指南

### 命名约定

1. **概念名称**：使用英文大驼峰（PascalCase），如 `Consensus`、`ConsensusRelation`
2. **字段名称**：使用英文小写字母和下划线（snake_case），如 `created_at`、`relation_type`
3. **事件名称**：使用英文大驼峰（PascalCase），如 `ConsensusCreated`、`ConsensusRelationCreated`

### 定义内容

1. **定义部分**：清晰定义概念是什么，包含哪些特征
2. **领域模型**：定义数据结构和字段，使用列表格式
3. **领域事件**：定义系统中发生的事件，包括发生时机、事件载荷和下游影响
4. **API规格**：定义接口规范，包括使用场景、请求/响应示例

### 示例数据

1. **使用实际数据**：JSON示例使用实际数据，体现真实场景
2. **时间戳**：使用ISO 8601格式，如 `2026-08-28T14:30:00+08:00`
3. **UUID**：使用实际UUID格式，如 `c0a80101-0000-0000-0000-000000000001`
4. **关联关系**：示例数据应体现概念之间的关联关系

## 目录结构

```
docs/specification/
├── AGENTS.md              # 本文件
├── README.md              # 项目说明
├── LICENSE                # 许可证
├── context/               # 语境定义
│   └── index.md           # 语境定义
├── medium/                # 媒介定义
│   ├── index.md           # 媒介定义
│   ├── message.md         # 消息定义
│   ├── memo.md            # 备忘定义
│   └── email.md           # 邮件定义
└── content/               # 内容定义
    ├── index.md           # 内容定义
    └── consensus.md       # 共识定义
```

## 变更流程

1. **理解需求**：明确要修改或添加什么内容
2. **查看现有文档**：了解定义风格和格式规范
3. **修改文档**：遵循文档结构规范和格式规范
4. **更新CHANGELOG.md**：记录变更内容
5. **提交变更**：在子模块中提交并推送
6. **更新父仓库引用**：回到父仓库更新子模块引用
7. **提交父仓库变更**：提交并推送父仓库变更
